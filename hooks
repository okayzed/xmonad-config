#!/usr/bin/bash

if [[ $1 == "startup" ]]; then
  # Apps
  guake &

  # network manager
  killall nm-applet
  nm-applet &

  # lock screen
  gnome-screensaver &

  # docks
  xmobar &

  killall trayer
  trayer --edge top --align right --SetDockType true --SetPartialStrut true  --width 10 --transparent true --tint 0x000000 --height 24 &

  gnome-settings-daemon &
fi
